
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow any authenticated user to read/write to any document.
    // This is permissive and suitable for initial development but should be
    // locked down before production.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
